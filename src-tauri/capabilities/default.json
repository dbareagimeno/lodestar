{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos base de la ventana principal (core). Los comandos propios de lodestar no requieren capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
