//! Fachada de escritorio (Tauri v2) — **placeholder de E6**.
//!
//! El cableado real de Tauri (ventana, tabla de comandos `§7.1`, evento `bundle:changed`,
//! `ipc.ts` generado) requiere librerías de sistema (webkit) y se implementa en la épica E6.
//! De momento este binario solo verifica que el crate compone `core` + `workspace`.

fn main() {
    println!(
        "lodestar-desktop: placeholder de E6. La fachada Tauri se implementa en la fase 6 (§14)."
    );
}
