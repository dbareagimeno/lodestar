//! Fachada de agentes (MCP/rmcp/stdio) — **placeholder de E7**.
//!
//! El servidor MCP real (tools de semántica, resources, `commit` para agentes) se implementa
//! en la épica E7 sobre `lodestar-workspace`. Logs solo a stderr; stdout = JSON-RPC.

fn main() {
    eprintln!("lodestar-mcp: placeholder de E7. El servidor MCP se implementa en la fase 7 (§14).");
}
